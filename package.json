{
  "name": "next-amp",
  "version": "1.0.0",
  "description": "NextStudio - Pitch Shifter, AI Vocal & Video Sync",
  "scripts": {
    "build": "npm run build:web && npm run build:extension",
    "build:web": "node scripts/build-web.js",
    "build:extension": "node scripts/build-prod.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^5.6.0"
  }
}
